# =============================================================================
# HateMirage ICON 2026 — Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# For GPU on Colab: faiss-gpu is pre-installed; replace faiss-cpu if needed.
# =============================================================================

# Core ML
pandas>=2.0.0
torch>=2.1.0
transformers>=4.40.0
sentence-transformers>=2.2.2
numpy>=1.25.0
scikit-learn>=1.3.0

# Quantization (4-bit NF4 for Colab T4)
bitsandbytes>=0.43.0
accelerate>=0.30.0

# RAG / Retrieval
langchain>=0.1.0
langchain-community>=0.1.0
faiss-cpu>=1.7.4    # use faiss-gpu on GPU machines

# Evaluation
rouge-score>=0.1.2
nltk>=3.8.0

# Data / Utilities
openpyxl>=3.1.0
tqdm>=4.65.0
pyyaml>=6.0
